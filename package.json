{
  "name": "healthcare-agentic",
  "version": "8.0.0",
  "description": "AI-powered healthcare billing and insurance assistance platform with unified Team Tabs navigation",
  "scripts": {
    "generate-articles": "python scripts/generate-articles.py",
    "generate-articles-simple": "python scripts/generate_articles.py",
    "build": "npm run generate-articles && firebase deploy",
    "dev": "firebase serve",
    "deploy": "firebase deploy",
    "validate-articles": "python -c \"import sys; sys.path.insert(0, 'scripts'); from generate_articles import ArticleGenerator; ArticleGenerator('public/article-template.html', 'public/articles-data.json', 'public/resources').generate_articles()\""
  },
  "dependencies": {
    "markdown": "^3.4.0"
  },
  "devDependencies": {
    "firebase-tools": "^12.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
} 